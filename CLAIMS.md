# 논리적 주장 — v12mix hub 실측 기준

작성: 2026-09-24  
범위: Fig1–7. 게이트 `runs/paper/v12mix/GATE.txt`, `runs/paper/v12mix_hub/GATE.txt`, `GATE_C2.txt`, `results/GATE_v12mix_strict.txt`, Fig7 CSV `v12mix_hub/fig7/`.  
제안 허브: 공유 MoE(눈·fc2 공유, 조타 헤드 5) + 학습 soft route-mix=0.15(평가는 hard) + Message/Critic 단일 + 타이 인코더. **잔차 Δμ 아님.**  
태그: hub ON `qd_MOE_SE_MX`, SINGLE `q_MOE_SINGLE_MX`. ablation은 `v12mix_hub/`.  
**PRIMARY v2-strict** (`OFF_DEG=15`, `SPD_DROP=0.20`, `SO_NULL_Q=60`). 근접=미완 포함. FINAL 미기록.

공통 전제

- Fig1–7은 **같은 v12mix 레시피** 위에서 축 하나만 바꾼다 (Fig2 C2의 THIN/THICK만 타이 인코더 OFF — 폭 축소와 타이 충돌).
- 도착(goal)=종료 에피소드 분모. 근접(prox)=OBB 겹침 종료 / (종료+창끝 미완).
- PRIMARY v2-strict C는 hub ~96% (dominant). **조건 선택은 도착·근접, C는 Fig5에서만.**
- 선박 16, 해역 600×600, ring 0.7, 레이더 56 m ≪ 통신 420 m.

---

## 교차 주장

### C1. 통신은 부분관측을 메운다
ON이 OFF보다 도착↑·근접↓이면 채널이 쓰인다. C로 증명하지 않는다.

### C2. 인식부를 상황별로 복제하면 실패한다
THICK/THIN(인코더 분리)이 SHARED보다 나쁘면, 원인은 용량이 아니라 **쪼갠 인식**이다.

### C3. 규정 항은 항해가 아니라 역할 분담을 산다
규정 계수를 **처음부터** 넣으면 C가 높다. **중간에 올리는** late-ramp는 도착·근접은 동등 이상이나 C는 early보다 낮다. “안전과 준수를 동시에”는 과장이다.

### C4. 종료만 모은 겹침은 붕괴가 아니다
미완 포함 근접은 hub ~1.5%.

### C5. PRIMARY v2-strict는 소프트 천장을 깬다
구 v2(~98%)보다 문턱을 조였다. hub C_dom≈96%. ON/OFF·DIM은 여전히 C로 거의 안 갈린다.

---

## Fig1 — 통신이 필요한가

**축:** 같은 hub. ON vs 학습 OFF (`qf_SE_OFF_MX`). PRIMARY v2-strict.

| | 도착 | 근접 | C_v2 |
|--|------|------|------|
| OFF | 92.7±2.8 | 2.5±0.7 | 95.5 |
| ON (hub) | **95.7±0.9** | **1.5±0.6** | 96.2 |

**주장:** 통신은 도착 +3.0pp, 근접 −1.0pp (시드 2/3).  
**버리지 말 것:** C로 통신 이득을 말하지 않는다.

---

## Fig2 — 전문화를 어디서 하나

**축:** SINGLE / THIN(폭 0.44·분리) / THICK(폭 1.0·분리) / SHARED(공유+route-mix).

| | 도착 | 근접 | C_v2 |
|--|------|------|------|
| SHARED (hub) | **95.7±0.9** | **1.5±0.6** | 96.2 |
| SINGLE | 95.6±1.3 | 1.7±0.5 | 94.7 |
| THICK | 92.3±0.8 | 2.5±0.3 | ~96 |
| THIN | 87.4±0.2 | 4.5±0.2 | ~97 |

**주장:** SHARED ≥ SINGLE (goal+prox, 시드 2/3). SHARED ≫ THIN/THICK (C2).  
**메커니즘:** 눈은 공유, 조타만 상황 라우팅. 학습 때 soft mix로 희소 헤드에 그라디언트. Unique params: SHARED≈SINGLE(~370K), THICK는 인코더 복제.  
**버리지 말 것:** 잔차 Δμ를 MoE라고 쓰지 않는다. YHSH 57%/1.83% 서사 금지.

---

## Fig3 — 이웃을 몇 척 듣나

**축:** hub K=4 vs `qf_SE_NEAR1_MX`(학습·평가 K=1).

| | 도착 | 근접 |
|--|------|------|
| K=4 | **95.7** | **1.5** |
| K=1 | 94.6 | 1.9 |

**주장:** 4척이 1척보다 낫다 (dGoal +1.1, dProx −0.4).

---

## Fig4 — 메시지가 얼마나 넓어야 하나

**축:** DIM ∈ {2,4,6,8,10,12} (같은 mix MoE). Unique params ~215–223K — **차원↑ ≠ 용량 폭증**.

| DIM | 도착 | 근접 | params |
|-----|------|------|--------|
| 2 | 84.1±13.0 | 7.0±6.5 | ~215K |
| 4 | 93.0±2.7 | 2.6±0.9 | ~217K |
| **6** | **95.7±0.9** | **1.5±0.6** | ~218K |
| 8 | 94.1±1.6 | 2.2±0.6 | ~220K |
| 10 | 94.2±1.6 | 2.0±0.5 | ~221K |
| 12 | 92.7±1.5 | 2.9±0.7 | ~223K |

**주장:** DIM2/4는 채널이 너무 좁다. DIM≥6에서 평탄·소폭 악화 → **이 과제에서 6이 충분, 그 이상은 이득 없음.**  
**버리지 말 것:** “차원↑로 용량이 커져서 나빠졌다”는 핑계 (param 증분은 무시 가능).

---

## Fig5 — 규정 계수를 언제 올리나

**축:** 구조·통신 동일. early = 처음부터 `coef=0.45`(hub). late = 0 → **9M에서 0.45** (`qo_SE_COLREGS_LATE_MX`).  
(참고: 끝까지 0인 `COLREGSOFF`는 본문 메인 바가 아님.)

| | 도착 | 근접 | C_v2 |
|--|------|------|------|
| late @9M | **96.4±1.7** | **1.3±0.7** | 90.9 |
| early @0 (hub) | 95.7±0.9 | 1.5±0.6 | **96.2** |

**주장:** 항해가 선 뒤에 규정을 올리면 도착·근접은 early와 동등 이상. C는 early가 높다. 처음부터 켜도 붕괴는 없다.  
**버리지 말 것:** 전면 OFF(C≈66) 패널로 “왜 v2냐”처럼 읽히게 만들지 않는다. C만으로 조건을 고르지 않는다.

---

## Fig6 — 통신을 언제 켜나

**축:** `comm_on_at` 9M(hub) vs 0 (`qo_SE_COMM0_MX`).

| | 도착 | 근접 |
|--|------|------|
| @9M | **95.7** | **1.5** |
| @0 | 93.1 | 2.5 |

**주장:** 늦게 켜는 쪽이 낫다. 처음부터 켜도 시드 붕괴는 없다.

---

## Fig7 — 통신 능력이 섞이면

**축:** 재학습 없음. hub 고정. n_rx=0…16이 **송신만 불가(수신 OK)**. mode=rx.  
그림 패널: (a) 구성 (b) prox (c) goal (d) minSep. step-legacy C는 본문 메인 아님.

**실측 (3시드, 함대 eps 가중):** 도착 ~93–98%, coll/prox ~2–7%, minSep ~19–21 m. n_rx에 단조 급락 없음.

**주장:** 수신이 살아 있으면 송신 척수를 줄여도 함대가 급락하지 않을 수 있다. **송신 희소 ≠ 채널 차단** (Fig1 OFF=메시지 전부 0과 다름).  
**버리지 말 것:** YHSH 충돌 1–2% 곡선. step-legacy C(~63%)로 PRIMARY를 대체 주장하지 않는다.

---

## Fig8

이 freeze 재평가 없음(이전 결정: 그리지 않음).

---

## 논문에 남길 / 지울 문장

남긴다

1. 통신은 도착·근접을 개선한다 (Fig1).
2. 공유 MoE(+route-mix)가 단일망을 이기고, 인식 복제(THIN/THICK)는 실패한다 (Fig2).
3. 이웃 4척 > 1척 (Fig3).
4. 메시지 차원 6이면 충분; DIM&lt;6은 부족, DIM&gt;6은 이득 없음 (Fig4). 차원↑≠용량↑.
5. 규정 계수는 항해 뒤에 올려도 되고, early가 C는 더 높다 (Fig5).
6. 통신은 항해 뒤에 켜는 편이 낫다 (Fig6).
7. 듣기만 가능한 배가 있으면 송신 희소는 치명적이지 않을 수 있다 (Fig7).

지운다

1. 잔차 Δμ가 제안 MoE다.
2. 통신이 v2 준수를 두 자릿수 pp로 올린다.
3. 규정 항이 충돌을 크게 줄인다.
4. 처음부터 통신을 켜면 붕괴한다.
5. 통통배 비율이 성능을 단조 악화한다.
6. “메시지 차원이 커져 파라미터가 폭증해서” Fig4가 나빠졌다.
